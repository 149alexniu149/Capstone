for(int i = 0; i<listliength;i++)
{
	if(list[i] == key)
	{
		return true
	}
}
l=0
mid = 0
while l<list.length
	l = l+list.lentgh/2
	if key < key
	for int i = l
	
	collision, when two records are hashed to the saem address
	
	collision handling
	
	linear probing to handle collisions: in the efent of collision, find the next available location for the data
		-	search time increases linearly
	KNOW HOW TO CREATE HASH TABLES